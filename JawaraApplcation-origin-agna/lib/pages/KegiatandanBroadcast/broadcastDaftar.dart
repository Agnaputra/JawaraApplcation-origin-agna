import 'package:flutter/material.dart';

class boradcastDaftarPage extends StatelessWidget {
  const boradcastDaftarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Broadcast Daftar',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
