import 'package:flutter/material.dart';

class TambahPages extends StatelessWidget {
  const TambahPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Tambah',
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}
