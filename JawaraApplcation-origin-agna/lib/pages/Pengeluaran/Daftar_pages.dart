import 'package:flutter/material.dart';

class DaftarPages extends StatelessWidget {
  const DaftarPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Daftar',
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}
