import 'package:flutter/material.dart';

/// Minimal application styles used by pages in the app.
class AppStyles {
  AppStyles._();

  /// Primary color used across the app. Adjust to match your design system.
  static const Color primaryColor = Color(0xFF1E88E5); // blue
}
